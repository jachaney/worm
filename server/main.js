import { Meteor } from 'meteor/meteor';

import '../imports/api/accountvalidation';
import '../imports/api/contacts';
import '../imports/api/workorders';

Meteor.startup(() => {
  // code to run on server at startup
});
